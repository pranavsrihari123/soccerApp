buildscript {
    ext {
        buildToolsVersion = "33.0.0"
        minSdkVersion = 21
        compileSdkVersion = 33
        targetSdkVersion = 33
        ndkVersion = "23.1.7779620"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:7.1.0") // Use a version compatible with Java 11 or later
        classpath("com.facebook.react:react-native-gradle-plugin:0.71.19") // Use a version compatible with Java 11 or later
    }
}
